name: Lint

on:
  push:
    branches: [main]
  pull_request:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          persist-credentials: false

      - name: Setup pnpm
        uses: pnpm/action-setup@0ebf47130e4866e96fce0953f49152a61190b271

      - name: Setup Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Setup Biome
        uses: biomejs/setup-biome@4c91541eaada48f67d7dbd7833600ce162b68f51

      - name: Run Biome
        run: biome ci .

      - name: Validate skills
        run: pnpm validate:skills

      - name: Build
        run: pnpm build

      - name: Validate lockfile
        run: pnpm lint:lockfile


  react-doctor:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
      issues: write
      statuses: write
    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1
        with:
          fetch-depth: 0
          persist-credentials: false

      - name: Read catalog react-doctor version
        id: catalog
        run: |
          version="$(node -e '
            const text = require("fs").readFileSync("pnpm-workspace.yaml", "utf8");
            const match = text.match(/^\s*"?react-doctor"?\s*:\s*"?([^"\s#]+)"?/m);
            if (!match) {
              console.error("react-doctor missing from pnpm-workspace.yaml catalog");
              process.exit(1);
            }
            process.stdout.write(match[1]);
          ')"
          echo "version=${version}" >> "$GITHUB_OUTPUT"

      - name: Run React Doctor
        uses: millionco/react-doctor@01820bb4fd4d0a4aebcd8df2b2a143a098649cb2
        with:
          version: ${{ steps.catalog.outputs.version }}
          blocking: error
          node-version: "22"